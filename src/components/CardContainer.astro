---
import PostCard from './PostCard.astro';
const { filteredPosts } = Astro.props;
---

<ul class='cardContainer space-cards'>
	{
		filteredPosts.map(
			(post: {
				frontmatter: {
					image: string;
					title: string;
					pubDate: string;
					author: string;
					description: string;
					tags: string;
				};
			}) => <PostCard {post} />
		)
	}
</ul>
